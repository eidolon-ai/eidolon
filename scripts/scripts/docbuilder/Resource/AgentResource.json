{
  "$defs": {
    "Metadata": {
      "additionalProperties": true,
      "properties": {
        "name": {
          "default": "DEFAULT",
          "title": "Name",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "title": "Metadata",
      "type": "object"
    }
  },
  "properties": {
    "apiVersion": {
      "title": "ApiVersion",
      "const": "server.eidolonai.com/v1alpha1"
    },
    "kind": {
      "const": "Agent",
      "default": "Agent",
      "title": "Kind"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/$defs/Metadata"
        }
      ],
      "default": {
        "name": "DEFAULT"
      }
    },
    "spec": {
      "$ref": "../schemas/Agent/overview.json"
    }
  },
  "required": [
    "apiVersion",
    "kind",
    "spec"
  ],
  "title": "AgentResource",
  "type": "object"
}