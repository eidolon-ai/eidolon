{
  "$defs": {
    "Metadata": {
      "additionalProperties": true,
      "properties": {
        "name": {
          "default": "DEFAULT",
          "title": "Name",
          "type": "string"
        }
      },
      "title": "Metadata",
      "type": "object"
    }
  },
  "properties": {
    "apiVersion": {
      "title": "Apiversion",
      "type": "string"
    },
    "kind": {
      "const": "Agent",
      "default": "Agent",
      "enum": [
        "Agent"
      ],
      "title": "Kind",
      "type": "string"
    },
    "metadata": {
      "allOf": [
        {
          "$ref": "#/$defs/Metadata"
        }
      ],
      "default": {
        "name": "DEFAULT"
      }
    },
    "spec": {
      "$ref": "../schemas/Agents.overview.json"
    }
  },
  "required": [
    "apiVersion",
    "spec"
  ],
  "title": "AgentResource",
  "type": "object"
}